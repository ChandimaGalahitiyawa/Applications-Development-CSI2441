<%= form_with(model: philosopher, local: true, html: { class: "mt-4" }) do |form| %>
  <% if philosopher.errors.any? %>
    <div class="alert alert-danger">
      <h2><%= pluralize(philosopher.errors.count, "error") %> prohibited this philosopher from being saved:</h2>

      <ul>
        <% philosopher.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= form.label :first_name, class: "font-weight-bold" %>
    <%= form.text_field :first_name, class: "form-control" %>
  </div>

  <div class="form-group mt-4">
    <%= form.label :last_name, class: "font-weight-bold" %>
    <%= form.text_field :last_name, class: "form-control" %>
  </div>

  <div class="form-group mt-4">
    <%= form.label :birth_year, class: "font-weight-bold" %>
    <%= form.number_field :birth_year, class: "form-control" %>
  </div>

  <div class="form-group mt-4">
    <%= form.label :death_year, class: "font-weight-bold" %>
    <%= form.number_field :death_year, class: "form-control" %>
  </div>

  <div class="form-group mt-4">
    <%= form.label :biography, class: "font-weight-bold" %>
    <%= form.text_area :biography, class: "form-control" %>
  </div>

  <div class="text-center mt-4">
    <%= form.submit "Save", class: "btn btn-primary custom-btn " %>
  </div>
<% end %>
